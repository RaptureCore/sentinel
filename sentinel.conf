# specify path to rapture.conf or leave blank
# default is the same as RaptureCore
#rapture_conf=~/.rapturecore/rapture.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
